name: Build Android TV Player

on:
  workflow_dispatch:
  push:
    tags:
      - 'android-tv-v*'
    paths:
      - 'android-tv/**'

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
      SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
      # Moved to job-level env (from per-step env) so that each step's `if:
      # env.KEYSTORE_BASE64 != ''` condition can reliably see it. A step's own
      # `env:` block is only guaranteed to be visible to *its own* run steps,
      # not necessarily evaluated the same way for that step's `if:` gate on
      # every runner/actions version — job-level env removes the ambiguity.
      KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Make gradlew executable
        run: chmod +x android-tv/gradlew

      - name: Decode keystore
        if: env.KEYSTORE_BASE64 != ''
        run: |
          echo "$KEYSTORE_BASE64" | base64 -d > android-tv/app/release.keystore

      - name: Build Release APK (signed)
        if: env.KEYSTORE_BASE64 != ''
        working-directory: android-tv
        run: |
          ./gradlew assembleRelease \
            -Pandroid.injected.signing.store.file=$PWD/app/release.keystore \
            -Pandroid.injected.signing.store.password=${{ secrets.KEYSTORE_PASSWORD }} \
            -Pandroid.injected.signing.key.alias=${{ secrets.KEY_ALIAS }} \
            -Pandroid.injected.signing.key.password=${{ secrets.KEY_PASSWORD }}

      - name: Build Release AAB (signed)
        if: env.KEYSTORE_BASE64 != ''
        working-directory: android-tv
        run: |
          ./gradlew bundleRelease \
            -Pandroid.injected.signing.store.file=$PWD/app/release.keystore \
            -Pandroid.injected.signing.store.password=${{ secrets.KEYSTORE_PASSWORD }} \
            -Pandroid.injected.signing.key.alias=${{ secrets.KEY_ALIAS }} \
            -Pandroid.injected.signing.key.password=${{ secrets.KEY_PASSWORD }}

      - name: Build Debug APK
        working-directory: android-tv
        run: ./gradlew assembleDebug

      - name: Build Debug AAB
        working-directory: android-tv
        run: ./gradlew bundleDebug

      - name: Rename artifacts for clarity
        run: |
          TAG="${GITHUB_REF_NAME:-dev}"
          # Rename debug APK with -debug suffix so it is distinguishable from release
          for f in android-tv/app/build/outputs/apk/debug/*.apk; do
            if [ -f "$f" ]; then
              mv "$f" "android-tv/app/build/outputs/apk/debug/DigipalPlayer-${TAG}-debug.apk"
            fi
          done
          # Rename debug AAB
          for f in android-tv/app/build/outputs/bundle/debug/*.aab; do
            if [ -f "$f" ]; then
              mv "$f" "android-tv/app/build/outputs/bundle/debug/DigipalPlayer-${TAG}-debug.aab"
            fi
          done
          # Rename release APK if exists
          for f in android-tv/app/build/outputs/apk/release/*.apk; do
            if [ -f "$f" ]; then
              mv "$f" "android-tv/app/build/outputs/apk/release/DigipalPlayer-${TAG}.apk"
            fi
          done
          # Rename release AAB if exists
          for f in android-tv/app/build/outputs/bundle/release/*.aab; do
            if [ -f "$f" ]; then
              mv "$f" "android-tv/app/build/outputs/bundle/release/DigipalPlayer-${TAG}.aab"
            fi
          done

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: android-tv-player-apk
          path: |
            android-tv/app/build/outputs/apk/release/*.apk
            android-tv/app/build/outputs/apk/debug/*.apk
          if-no-files-found: warn

      - name: Upload AAB
        uses: actions/upload-artifact@v4
        with:
          name: android-tv-player-aab
          path: |
            android-tv/app/build/outputs/bundle/release/*.aab
            android-tv/app/build/outputs/bundle/debug/*.aab
          if-no-files-found: warn

  release:
    if: startsWith(github.ref, 'refs/tags/android-tv-v')
    needs: [build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/**/*.apk
            artifacts/**/*.aab
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
