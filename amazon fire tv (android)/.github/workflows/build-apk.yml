name: Build Amazon Fire TV APK

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Build debug APK
        run: ./gradlew assembleDebug

      - name: Rename APK
        run: |
          mkdir -p release
          cp app/build/outputs/apk/debug/app-debug.apk release/NexusCast-amazon-fire-tv.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v5
        with:
          name: NexusCast-amazon-fire-tv
          path: release/NexusCast-amazon-fire-tv.apk
          retention-days: 90

      - name: Create Release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: amazon-fire-tv-v3.0.${{ github.run_number }}
          name: NexusCast TV v3.0.${{ github.run_number }}
          files: release/NexusCast-amazon-fire-tv.apk
          make_latest: true
          body: |
            NexusCast TV v3.0.${{ github.run_number }}
            
            Includes native local media storage for offline playback.
            
            Built from commit ${{ github.sha }}
